{
  "name": "cqnu-plant-map-site",
  "version": "1.1.0-beta.2",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm --prefix ../admin run build",
    "build": "node --disable-warning=ExperimentalWarning scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run build && npm run validate",
    "package:sites": "npm run check && node scripts/package.mjs",
    "prestart": "npm run build",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
